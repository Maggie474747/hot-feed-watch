name: Daily Hot Feed Fetch

on:
  schedule:
    - cron: "0 0,9 * * *" # 北京时间 08:00 和 北京时间17:00执行
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-and-update:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Ensure data directory exists
        run: mkdir -p data

      - name: Run fetch scripts (without TikTok)
        run: |
          python fetch_bilibili.py || echo "bilibili failed"
          python fetch_weibo.py || echo "weibo failed"
          python fetch_reddit.py || echo "reddit failed"

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No data changes to commit."
          else
            git commit -m "chore(data): daily update hot feeds"
            git push
          fi